session_name: blog
windows:
- window_name: blog dev
  layout: main-horizontal
  panes:
    - shell_command:
      - code .
      - sleep 3
      - open http://localhost:4000/blog/
      - clear
      focus: true
    - shell_command:
      - eval "$(rbenv init -)"
      - bundle exec jekyll serve

